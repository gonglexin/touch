<%= render 'shared/breadcrumb', :parent => @category.parent %>

<div class="alert-message success">
  <p id="notice"><%= notice %></p>
</div>

<p>
  <b>Name:</b>
  <%= @category.name %>
</p>

<%= link_to 'Edit', edit_admin_category_path(@category) %>
